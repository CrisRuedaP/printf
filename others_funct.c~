#include <stdio.h>
/**
 * main - Is the principal function
 *
 * Return: Always 0
 */
int main(void)

{
  int x;
  for (x = '0'; x <= '9'; x++)
    {
      putchar(x);
    }
  putchar ('\n');
  return (0);
}

/**
 * _strlen - return the length of a string
 * @s: string
 * Return: result.
 */
int _strlen(char *s)
{

  int accum = 0;

  while (s[accum] != '\0')

    accum++;

  return (accum);

}
